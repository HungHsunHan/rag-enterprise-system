# Backend / LLM
OPENROUTER_API_KEY = your_openrouter_api_key_here

# Frontend API base URL
# - Dev: leave unset to use Vite dev proxy (requests go to relative "/api/...").
# - Prod (same-origin or behind reverse proxy): leave unset or set to "/".
# - Prod (separate API origin): set to full origin (no trailing slash), e.g. "https://api.example.com".
#   The app will call `${VITE_API_BASE_URL}/api/v1/...`.
# Examples:
# VITE_API_BASE_URL=https://api.example.com
# VITE_API_BASE_URL=/
# VITE_API_BASE_URL=
